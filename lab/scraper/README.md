# Scraper Lab

Ad hoc scraper investigation files.

This directory is for debugging CUHK page-structure changes, saved sample pages, captcha samples, temporary scripts, trial outputs, and logs. It is not the active test suite and is not used by the production scraper or web app.

Keep stable examples in `samples/`. Generated outputs and logs can be removed when they are no longer useful.
